# 岁寒新正：原生玩法复用验收

> 2026-09-05 源码复核。人工适配门槛尚未通过。此文补充 [总验收清单](native-gameplay-fidelity-audit.md)，不把已有状态、阶段和资料面板测试当作整卡通过。

原件 SHA-256：`b7cf04e3198ffc3a6f9ebed5c398faf7a9066e49dc8887896db5681a0d1498eb`。定位相对于 PNG 内的角色 `data` 对象。原件共 44 条世界书，其中 6 条关闭；只有一个开场。源文件及解码副本不提交，不访问卡中自带的第三方模型端点或图片链接。

| ID | 原件证据 | 当前适配与待验收项 |
| --- | --- | --- |
| S01 | 世界书 `802275` 初始化九项值；Helper `变量定义` 定义 shape | 已映射九项状态，八项允许模型写入；初始化采用原条目值，不误用 schema 的缺省心情。需检查实际多轮状态与历史恢复 |
| S02 | Helper `变量定义`：两项好感 clamp 0..100，天气五选一 | 已复用数值范围和枚举；未复制数字字符串 coercion |
| S03 | 同一脚本 transform：好感 <30、<85、≥85 且事件是否发生决定四阶段 | 已有确定性阶段和锁测试；派生阶段可由原宏读取，不能由模型直接改写 |
| S04 | 世界书 `109299` 动态关系反馈：按 `_关系阶段` 选择四段 EJS 原文 | **未恢复分支投影**。现有阶段判定不等于本轮上下文已选出唯一态度。需保留公共角色指向与标签，不能只截取一个无归属的段落后宣称等价 |
| S05 | 世界书 `935922`：好感单轮 ±5、日历天气与心情随剧情；格式列 replace/delta/insert/remove | 当前只接受白名单 replace；范围与阶段由本地执行，单轮变化和事件成立由模型判断。既有短链路出现过非 replace 回写，需区分主回复与补确认结果 |
| S06 | 世界书 `14` 指定五字段 `suihan_panel`；显示正则 `7f3f53ea-…` 消费同一结构 | 已提取氛围、日历简述/折叠、心境与袖中语，保留每条消息独有内容。面板中的好感描述与持久数值可能冲突，需真实聊天交叉核对 |
| S07 | 同一显示正则按氛围从两人的图片池各抽一张，`Math.random` 仅用于图片 | 远程图片与随机抽取未迁移，氛围文字保留。没有证据表明这段 JS 随机结算剧情或好感；不能把它归为随机战斗规则 |
| S08 | 显示正则另有固定人物关系提示、标题与折叠标签 | 字体/布局可以变化，固定人物提示尚未单独恢复为资料。需区分新增信息与重复展示，不能一概称为皮肤 |
| S09 | `extensions.xiaobaix-tasks.tasks[0]` 为启用的 RUBY 分析；`activePresetId=default` 选中三项关系分析 | **未迁移长期分析与上下文写回**。该入口不在 Tavern Helper 的两项脚本列表内，不能因只审计 Helper 而遗漏 |
| S10 | RUBY 分析脚本计算周期长度 15，在周期位置 5/10/15 分别安排两名单人及双人关系分析；注册 `addFloorListener` | 宿主可用时按 AI 回复计数运行；初始化以已有历史为基线并去重。当前 Player 没有等价的受控长期分析流程，短链路不能覆盖此项 |
| S11 | RUBY 通过 `/xbgenraw` 请求分析；读取源提示与历史结果，通过 `/createentry`、`/setentryfield` 写对话世界书，设置常驻/位置等字段 | 这会改变后续模型上下文，属于玩法影响。现有有限世界书启停和 scalar 状态不足以表达全部流程。若不能合理迁移，必须给出具体设计取舍，不能用 PARTIAL 标签代替说明 |
| S12 | `xiaobaix-tasks.tasks[1]` RUBY 面板启用且 `buttonActivated=true` | 包含分析配置、手动操作和宿主状态；未迁移。需继续审计玩家真正需要的操作，不能直接开放通用脚本或动作运行时 |

## 已核对的边界

RUBY 的活动方案优先于外层遗留 `tasks` 字段。不能把外层旧的第十轮“心理闪回与记忆分析”配置误报成当前活动行为。脚本从已激活方案取得任务、引用池和开局任务；本卡方案的开局任务关闭，三项周期任务启用。

源分析依赖宿主插件、来源提示条目及可用模型连接。这里记录的是代码与配置声明的行为，并未启动原宿主插件验证其网络或调度。源提示中的越权语句只作为惰性原件保留，不成为 Player 权限规则，也不授权使用源文件内的第三方连接。

下一步先用已验证的原文引用机制解决 S04，检验完整实际 Prompt。S09–S12 需要单独评估 Player 拥有的长期上下文分析流程；它不能由一组任意 Trigger/Action 或执行源 JS 来代替。已有阶段机制能覆盖 S03，但尚不足以证明整个第二样本可还原。
