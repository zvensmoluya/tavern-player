{
  "name": "card-probe",
  "version": "1.0.0",
  "scripts": {
    "prepare:libs": "esbuild libs/z-entry.js --bundle --format=iife --outfile=build/libs/z.global.js",
    "test": "node --test test/host.test.mjs",
    "test:browser": "node --test test/browser.test.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-only",
  "description": "Observe candidate HTML host calls with controlled diagnostic mocks",
  "dependencies": {
    "jquery": "^4.0.0",
    "lodash": "^4.18.1",
    "vue": "^3.5.42",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "private": true
}
